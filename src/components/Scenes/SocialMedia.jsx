import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

const SocialMedia = () => (
  <Card>
    <CardContent>
      <Typography variant="h6">Social Media</Typography>
      <Typography variant="body2">No Risk</Typography>
    </CardContent>
  </Card>
);

export default SocialMedia;
